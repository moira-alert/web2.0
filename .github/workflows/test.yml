name: Test
on:
  pull_request:
jobs:
  test:
    name: Test frontend
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-node@v3
        with:
          node-version: "16"
          cache: "yarn"

      - name: Install dependencies
        run: yarn install

      - name: Run unit tests
        run: yarn test
  playwright:
    name: 'Playwright Tests'
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.40.0-jammy
      options: --user root
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: "16"

      - name: Change Yarn Rights
        run: sudo chmod -R 777 /root/.config/yarn

      - name: Install dependencies
        run: yarn install

      - name: Run screenshot tests
        run: yarn screenshots
        env:
          HOME: /root

      - name: Archive artifacts
        uses: actions/upload-artifact@v3
        if: ${{ always() }}
        with:
          name: screenshots-report
          path: test-results
